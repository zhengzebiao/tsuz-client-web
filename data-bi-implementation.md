# 数据与 BI 实施方案

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 数据与 BI 实施方案 |
| 适用范围 | 使用 `tsu-cli` 工具模板生成的 MFE 微前端项目 |
| 依据文档 | [prd.md](prd.md) / [technical-execution-plan.md](technical-execution-plan.md) |
| 当前前提 | 自建 BI、统一埋点、主应用统一登录、广告接入腾讯/百度 |
| 文档版本 | v0.1 |
| 日期 | 2026-06-22 |
| 目标读者 | 数据、BI、前端、后端、运营、商业化 |

---

## 2. 数据目标

- 建立统一事件采集规范。
- 建立自建 BI 原始数据链路。
- 支持登录、行为、广告三类数据统一分析。
- 支持登录转化、活跃、留存、子应用访问、广告曝光/点击统计。
- 为后续分群、归因、权限分析和广告收益分析留基础。

---

## 3. 数据架构

### 3.1 数据流

```txt
前端事件 -> Event Collector -> 原始事件表 -> 清洗表 -> 主题表 -> 指标看板
```

### 3.2 分层建议

| 层级 | 说明 |
| --- | --- |
| ODS | 原始事件 |
| DWD | 清洗明细 |
| DWS | 主题聚合 |
| ADS | 看板指标 |

---

## 4. 事件规范

### 4.1 公共字段

所有事件建议包含：

- `eventId`
- `eventName`
- `eventTime`
- `appId`
- `pageName`
- `routePath`
- `userId`
- `visitorId`
- `sessionId`
- `referrer`
- `utmSource`
- `utmMedium`
- `utmCampaign`
- `properties`

### 4.2 核心事件

P0 优先落地：

- `page_view`
- `login_start`
- `login_success`
- `login_fail`
- `logout`
- `permission_denied`
- `ad_request`
- `ad_impression`
- `ad_click`

---

## 5. 访客与用户关联

### 5.1 标识体系

| 标识 | 说明 |
| --- | --- |
| visitorId | 匿名访客 |
| sessionId | 会话 |
| userId | 登录用户 |
| deviceId | 可选设备标识 |

### 5.2 关联规则

登录成功后执行：

```txt
visitorId -> login_success -> userId
```

必要时可使用：

```ts
tracking.alias(visitorId, userId)
tracking.identify(userId, traits)
```

---

## 6. 指标体系

### 6.1 基础流量指标

- PV。
- UV。
- 页面访问排行。
- 来源渠道。

### 6.2 登录指标

- 登录入口点击率。
- 登录成功率。
- 登录失败率。
- 登录失败原因。
- 回跳成功率。

### 6.3 用户指标

- 新增用户。
- 活跃用户。
- 留存。
- 子应用使用深度。

### 6.4 广告指标

- 请求量。
- 填充率。
- 曝光量。
- 点击率。
- eCPM。
- 收入。

### 6.5 质量指标

- JS 错误率。
- API 错误率。
- 白屏率。
- 加载耗时。

---

## 7. BI 落地方式

### 7.1 MVP

先做：

- 原始事件入库。
- 登录转化看板。
- 基础流量看板。
- 子应用访问排行。
- 广告基础统计。

### 7.2 中期

- 用户主题表。
- 会话主题表。
- 页面主题表。
- 子应用主题表。
- 广告位主题表。

### 7.3 后期

- 用户分群。
- 漏斗分析。
- 归因分析。
- A/B 测试。
- 更细的广告收益分析。

---

## 8. 数据质量要求

- 事件命名统一。
- 公共字段完整。
- schema 变更有版本控制。
- 原始事件可追溯。
- 采集失败不影响主流程。
- 数据权限和审计要保留。

---

## 9. 运营与商业化看板建议

### 9.1 登录看板

- 登录入口点击。
- 登录成功率。
- 登录方式占比。
- 登录失败原因。

### 9.2 行为看板

- 页面访问排行。
- 子应用访问排行。
- 关键路径漏斗。

### 9.3 广告看板

- 广告位曝光。
- 广告位点击。
- 广告收益。
- 渠道表现。

---

## 10. 验收标准

1. 事件可稳定上报。
2. 登录前后可通过 visitorId/userId 关联。
3. 原始事件能入库。
4. 基础看板可展示。
5. 广告数据可纳入统一统计。
6. 后续可平滑扩展到主题表与分群。
