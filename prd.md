# MFE 微前端统一登录与用户行为数据系统 PRD

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | MFE 微前端统一登录与用户行为数据系统 PRD |
| 适用项目 | 使用 `tsu-cli` 工具模板生成的 MFE 微前端项目 |
| 文档版本 | v0.1 |
| 日期 | 2026-06-21 |
| 目标读者 | 产品、前端、后端、数据、增长、广告商业化、测试、运营 |

---

## 2. 背景与目标

### 2.1 背景

当前项目计划基于 `tsu-cli` 模板生成 MFE 微前端架构。系统中会存在多个子应用，不同子应用面向的业务场景、用户路径和登录要求可能不同：

- 部分子应用可以匿名访问，用于承接自然流量、SEO、广告落地页或轻量工具体验。
- 部分子应用需要用户强制登录后才能访问，例如账户中心、订单、数据看板、会员权益、敏感业务操作等。
- 登录入口和登录流程统一收敛在主应用，子应用不提供独立登录页面，只共享主应用维护的登录态。
- 需要记录用户在各子应用中的浏览、点击、转化、停留、错误、业务操作等行为，为后续 BI 分析、用户分层、漏斗分析、广告收入优化提供数据基础。
- 后续会接入第三方广告平台，需要支持广告曝光、点击、转化归因、收益分析，并尽量与用户行为数据打通。

### 2.2 产品目标

1. 建立统一登录体系，让用户在多个子应用之间获得连续、一致、低打扰的使用体验。
2. 支持不同子应用、不同页面、不同功能配置差异化登录策略。
3. 构建标准化用户行为采集体系，为 BI、增长、广告商业化、用户运营提供可信数据。
4. 为后续广告接入预留数据、权限、合规和收益归因能力。
5. 在不牺牲隐私合规和系统安全的前提下，提高用户转化、留存和商业化效率。

### 2.3 非目标

本阶段不直接实现完整广告投放系统，不自建 DMP 或复杂推荐系统，不建设完整企业级数仓。但需要在架构和数据埋点层面预留扩展能力。

---

## 3. 用户与业务视角

### 3.1 核心用户角色

| 角色 | 诉求 | 典型行为 |
| --- | --- | --- |
| 匿名访客 | 快速浏览内容、试用功能、减少注册阻力 | 浏览页面、搜索、点击广告、试用公开功能 |
| 注册用户 | 跨子应用保持登录、保存偏好、访问更多功能 | 登录、访问账户、使用核心功能、收藏、提交表单 |
| 高价值用户 | 稳定使用核心功能，获得个性化服务 | 高频访问、付费、深度互动、跨应用使用 |
| 运营人员 | 分析用户行为、提升转化和留存 | 查看漏斗、用户分群、活动效果 |
| 广告/商业化人员 | 分析广告曝光、点击、转化和收入 | 监控广告收益、广告位表现、转化归因 |
| 数据/BI 人员 | 获取稳定、可治理、可追溯的数据 | 建模、看板、指标体系、数据质量监控 |
| 管理员/安全人员 | 管理权限、审计操作、保障安全合规 | 权限配置、登录审计、异常行为分析 |

### 3.2 关键用户旅程

#### 3.2.1 匿名浏览到登录转化

1. 用户进入公开子应用或公开页面。
2. 系统分配匿名访客 ID。
3. 用户浏览内容、点击功能、触发关键行为。
4. 当用户访问强登录页面或触发强登录操作时，子应用向主应用发起登录请求。
5. 主应用统一展示登录页或登录弹窗，子应用不直接处理登录表单和认证逻辑。
6. 用户在主应用完成登录后，匿名行为与登录用户 ID 进行关联。
7. 主应用恢复原访问路径，并将最新登录态同步给当前子应用。

#### 3.2.2 跨子应用访问

1. 用户在主应用完成登录。
2. 用户从子应用 A 跳转到子应用 B。
3. 子应用 B 从主应用上下文中获取登录态，无需再次登录。
4. 若子应用 B 需要额外权限，则展示无权限页或引导申请权限。
5. 用户行为继续被统一采集并归因到同一用户。

#### 3.2.3 广告曝光到转化归因

1. 用户进入支持广告的页面。
2. 系统加载广告配置和第三方广告 SDK。
3. 记录广告请求、填充、曝光、点击等事件。
4. 用户点击广告或完成后续转化行为。
5. 数据侧支持按广告位、渠道、页面、用户分群、时间维度分析收益和转化。

---

## 4. 总体方案发散

### 4.1 登录系统方案

#### 方案 A：主应用统一登录与统一登录态管理（推荐）

**描述**

登录入口、登录页面/弹窗、登录接口调用、token 刷新、退出登录、用户信息拉取均由主应用负责。子应用不实现独立登录页面，也不直接处理账号、验证码、密码等认证表单。子应用只通过主应用提供的 `auth-sdk`、props、事件总线或微前端框架通信能力消费登录态，并在需要登录时调用主应用的 `requireLogin` 能力。

**适用场景**

- 登录明确收敛在主应用。
- 子应用数量较多，需要统一体验。
- 子应用需要共享用户状态，但不希望重复实现认证逻辑。
- 后续需要统一行为数据、权限、广告和 BI 归因。

**优点**

- 用户只认一个登录入口，体验清晰。
- 子应用接入成本低，不重复处理登录逻辑。
- 登录成功、失败、退出、token 刷新等关键事件天然集中，便于审计和 BI 分析。
- 便于统一接入风控、人机校验、隐私授权、第三方登录。
- 登录态变化可由主应用统一广播，降低跨子应用状态不一致风险。

**缺点**

- 主应用职责更重，需要保证稳定性。
- 子应用脱离主应用独立运行时需要 mock 登录态或开发态适配。
- 主应用登录模块异常会影响所有强登录子应用。

**建议**

将该方案作为明确主方案。认证能力集中在主应用，子应用只做登录态消费、权限判断展示和登录触发，不做真实登录。

#### 方案 B：独立 SSO 登录中心，主应用代理承接

**描述**

后续如果需要多端、多站点或多业务线统一账号体系，可建设独立 SSO 登录中心。但在当前 MFE 前端体验上，仍由主应用承接登录入口和登录态分发，子应用不直接跳转或接入 SSO。

**优点**

- 有利于中长期多端统一账号。
- 可扩展 OAuth、OIDC、企业 SSO、第三方登录。
- 认证服务职责更清晰。

**缺点**

- 初期建设成本更高。
- 主应用与 SSO 之间需要处理回跳、状态同步和异常兜底。

**建议**

作为中长期演进方向，不改变“用户在主应用登录、子应用消费登录态”的产品原则。

#### 方案 C：子应用独立登录（不采用）

**描述**

每个子应用直接接入认证服务，自己完成登录页、登录态检查、token 刷新、用户信息获取。

**不采用原因**

- 与“登录都在主应用”的产品原则冲突。
- 用户体验割裂，容易出现重复登录。
- 登录事件、权限、风控和 BI 归因难以统一。
- 子应用重复开发和维护成本高。

### 4.2 登录形态发散

当前阶段登录形态暂时收敛为三种：邮箱登录、账号登录、微信登录。其他登录方式暂不纳入 MVP 范围。

| 登录形态 | 说明 | 适用场景 | 优先级 |
| --- | --- | --- | --- |
| 邮箱登录 | 用户通过邮箱验证码或邮箱绑定账号完成登录 | 国内用户补充登录方式、账号找回、通知触达 | P1 |
| 账号登录 | 用户通过账号和密码完成登录 | 后台、管理端、老用户、测试账号 | P1 |
| 微信登录 | 用户通过微信扫码或授权完成登录 | 国内 C 端用户、微信生态流量 | P1 |

### 4.3 登录策略发散

| 策略 | 描述 | 示例 |
| --- | --- | --- |
| 强制登录 | 进入页面前必须登录 | 账户中心、订单、支付、管理后台 |
| 延迟登录 | 用户触发关键操作时再登录 | 收藏、评论、保存配置 |
| 可选登录 | 匿名可用，登录后体验更好 | 内容浏览、工具试用 |
| 静默登录 | 已有有效登录态时自动恢复 | 跨子应用访问 |
| 二次校验 | 敏感操作前再次确认身份 | 修改手机号、提现、删除数据 |

### 4.4 用户行为数据方案

#### 方案 A：前端统一埋点 SDK（推荐）

**描述**

建设统一 `tracking-sdk`，由主应用初始化，子应用按规范调用。SDK 负责自动采集基础事件，并支持业务手动埋点。

**自动采集**

- 页面访问 PV。
- 页面停留时长。
- 路由切换。
- 页面来源 referrer。
- 设备、浏览器、语言、屏幕信息。
- JS 错误和接口错误。
- 广告曝光基础事件。

**手动采集**

- 按钮点击。
- 表单提交。
- 登录成功/失败。
- 注册成功。
- 关键业务操作。
- 支付、订阅、下单、转化。

**优点**

- 统一数据口径。
- 易于治理和扩展。
- 降低子应用重复开发成本。

**缺点**

- 初期需要制定事件规范。
- 需要持续维护事件字典。

#### 方案 B：各业务自行埋点

**优点**

- 快速、灵活。

**缺点**

- 事件命名混乱。
- 字段缺失和重复严重。
- 后续 BI 成本高。

**建议**

不建议作为长期方案。允许业务自定义事件，但必须通过统一 SDK 和事件字典约束。

### 4.5 广告接入方案发散

#### 方案 A：广告 SDK 由主应用统一加载（推荐）

**描述**

主应用或公共基础层统一管理第三方广告 SDK 的加载、开关、权限、合规弹窗和广告位配置。子应用只声明广告位。

**优点**

- 易于统一控制广告开关。
- 避免多个子应用重复加载 SDK。
- 更好管理隐私授权和合规要求。
- 广告数据更容易统一归因。

**缺点**

- 主应用广告基础设施复杂度提高。

#### 方案 B：各子应用独立接入广告 SDK

**优点**

- 子应用上线广告速度快。

**缺点**

- 性能不可控。
- 合规风险更高。
- 数据口径不统一。

**建议**

MVP 可以选择一个子应用试点广告，但必须使用统一广告适配层，不建议子应用直接散落接入第三方 SDK。

---

## 5. 推荐总体架构

### 5.1 架构原则

1. 认证统一，权限可配置。
2. 登录态共享，子应用低耦合。
3. 埋点统一，事件可治理。
4. 匿名与登录用户可关联，但必须尊重隐私合规。
5. 广告接入平台化，不散落到业务代码。
6. 数据从第一天就面向 BI 建模。

### 5.2 模块划分

| 模块 | 职责 |
| --- | --- |
| 主应用 Shell | 微前端容器、全局路由、主应用登录页/弹窗、登录态初始化、token 刷新、全局配置、公共布局 |
| 子应用 | 业务功能实现，按规范声明登录策略、权限、埋点、广告位；不实现独立登录，只触发主应用登录 |
| Auth SDK | 主应用认证能力封装、登录态读取、权限判断、登录态监听、子应用 `requireLogin` 调用入口 |
| Tracking SDK | 用户行为采集、事件上报、公共字段注入、队列和失败重试 |
| Ads Adapter | 第三方广告 SDK 适配、广告位管理、曝光点击采集、开关控制 |
| Config Center | 路由登录策略、广告配置、埋点开关、灰度策略 |
| API Gateway/BFF | 统一接口鉴权、用户上下文注入、服务聚合 |
| Auth Service | 认证、授权、会话、token、账号体系 |
| Event Collector | 行为数据接收、校验、清洗、转发 |
| Data Warehouse/BI | 数据建模、指标体系、看板分析、用户分群 |

### 5.3 登录态流转

1. 用户访问主应用承载下的任意子应用路由。
2. 主应用初始化 `auth-sdk`，检查本地 token 或 session。
3. 若 token 有效，主应用拉取用户信息和权限，并注入到全局上下文。
4. 若 token 过期，主应用尝试静默刷新。
5. 若刷新失败，用户保持匿名态；当访问强登录页面或触发强登录操作时，由主应用展示登录页或登录弹窗。
6. 子应用根据主应用提供的登录态和路由策略决定是否允许继续访问。
7. 子应用需要登录时，只调用 `auth.requireLogin()` 或向主应用发送登录请求，不直接展示登录表单。
8. 用户在主应用完成登录后，主应用广播登录态变化，各子应用更新用户信息。
9. 用户退出登录时，由主应用清除登录态并广播退出事件。

### 5.4 子应用接入方式

每个子应用需要声明元信息：

```ts
export const appMeta = {
  appId: 'user-center',
  appName: '用户中心',
  auth: {
    required: true,
    fallback: 'redirect',
  },
  tracking: {
    enabled: true,
  },
  ads: {
    enabled: false,
  },
}
```

路由级别可以覆盖应用级别策略：

```ts
export const routes = [
  {
    path: '/profile',
    meta: {
      authRequired: true,
      permission: 'user.profile.read',
      eventPageName: 'profile_page',
    },
  },
  {
    path: '/landing',
    meta: {
      authRequired: false,
      adsEnabled: true,
      eventPageName: 'landing_page',
    },
  },
]
```

---

## 6. 功能需求

### 6.1 统一登录

#### 6.1.1 登录入口

- 所有登录入口统一由主应用提供，包括顶部全局登录入口、登录页和登录弹窗。
- 子应用不提供独立登录页面，不直接处理手机号、邮箱、验证码、密码、第三方登录回调等认证逻辑。
- 强登录页面访问时，子应用向主应用发起登录请求，由主应用自动跳转登录页或弹出登录弹窗。
- 登录完成后，主应用回跳用户原访问地址，并将登录态同步给当前子应用。
- 支持登录页携带来源参数，例如 `redirect_uri`、`app_id`、`campaign_id`、`from_route`。

#### 6.1.2 登录态共享

- 用户只在主应用完成登录，所有子应用自动感知主应用登录态。
- 同一浏览器会话中不重复登录。
- 支持刷新页面后由主应用恢复登录态。
- 支持多标签页登录态同步。
- 支持主应用统一退出登录，退出后所有子应用同步进入匿名态。

#### 6.1.3 登录失败处理

- 验证码错误、账号不存在、账号冻结、网络异常需要展示明确错误提示。
- 登录失败事件需要被记录，用于风控和转化分析。
- 多次失败可触发验证码、人机校验或临时锁定策略。

#### 6.1.4 登录后用户信息

至少包含：

| 字段 | 说明 |
| --- | --- |
| userId | 用户唯一 ID |
| nickname | 昵称 |
| avatar | 头像 |
| phone/email | 手机号或邮箱，前端脱敏展示 |
| roles | 用户角色 |
| permissions | 权限列表 |
| createdAt | 注册时间 |
| lastLoginAt | 最近登录时间 |
| userLevel | 用户等级或分层，后续可选 |

### 6.2 强制登录策略

#### 6.2.1 应用级强登录

某些子应用整体需要登录后访问，例如：

- 用户中心。
- 订单/支付。
- 会员中心。
- 管理后台。
- 私有数据看板。

#### 6.2.2 页面级强登录

同一子应用内部可存在公开页面和强登录页面，例如：

- `/landing` 可匿名访问。
- `/detail/:id` 可匿名访问。
- `/create` 必须登录。
- `/settings` 必须登录。

#### 6.2.3 操作级强登录

用户可浏览内容，但触发以下操作时要求登录：

- 收藏。
- 评论。
- 保存。
- 下载。
- 提交表单。
- 查看完整结果。
- 使用高级功能。

### 6.3 权限系统

#### 6.3.1 权限模型

建议采用 RBAC + 资源权限扩展：

- 用户 User。
- 角色 Role。
- 权限 Permission。
- 资源 Resource。
- 操作 Action。

示例：

```txt
user.report.read
user.report.export
admin.user.manage
ads.dashboard.read
billing.order.create
```

#### 6.3.2 权限校验位置

- 前端：用于菜单、按钮、页面展示控制，提升体验。
- 后端：用于真实安全校验，前端权限不可作为唯一安全边界。

### 6.4 用户行为采集

#### 6.4.1 用户标识

| 标识 | 说明 |
| --- | --- |
| visitorId | 匿名访客 ID，首次访问生成 |
| sessionId | 会话 ID，用于一次访问周期分析 |
| userId | 登录用户 ID |
| deviceId | 设备标识，可选，需注意合规 |
| anonymousId | 匿名行为归因 ID，可与 visitorId 合并 |

登录前后的行为需要支持关联：

```txt
visitorId -> login_success -> userId
```

#### 6.4.2 公共事件字段

每个事件默认包含：

| 字段 | 说明 |
| --- | --- |
| eventId | 事件唯一 ID |
| eventName | 事件名称 |
| eventTime | 客户端事件时间 |
| serverTime | 服务端接收时间 |
| appId | 子应用 ID |
| pageName | 页面名称 |
| routePath | 路由路径 |
| userId | 用户 ID，未登录为空 |
| visitorId | 访客 ID |
| sessionId | 会话 ID |
| referrer | 来源 |
| utmSource | UTM 来源 |
| utmMedium | UTM 媒介 |
| utmCampaign | UTM 活动 |
| device | 设备信息 |
| browser | 浏览器信息 |
| os | 操作系统 |
| screen | 屏幕信息 |
| locale | 语言地区 |
| network | 网络状态，可选 |
| properties | 事件自定义属性 |

#### 6.4.3 核心事件字典

| 事件名 | 触发时机 | 关键属性 |
| --- | --- | --- |
| `page_view` | 页面访问 | pageName、routePath、referrer |
| `page_leave` | 页面离开 | duration |
| `button_click` | 按钮点击 | buttonId、buttonName、moduleName |
| `login_start` | 开始登录 | loginMethod、sourceAppId |
| `login_success` | 登录成功 | loginMethod、isNewUser |
| `login_fail` | 登录失败 | loginMethod、reason |
| `logout` | 退出登录 | sourceAppId |
| `signup_success` | 注册成功 | signupMethod、channel |
| `permission_denied` | 权限不足 | permission、routePath |
| `api_error` | 接口异常 | apiPath、statusCode、errorCode |
| `js_error` | 前端异常 | message、stack、pageName |
| `ad_request` | 请求广告 | adSlotId、provider |
| `ad_impression` | 广告曝光 | adSlotId、provider、creativeId |
| `ad_click` | 广告点击 | adSlotId、provider、creativeId |
| `conversion` | 转化行为 | conversionType、value、currency |

### 6.5 BI 数据建设

#### 6.5.1 指标方向

| 指标类别 | 示例指标 |
| --- | --- |
| 流量指标 | PV、UV、访问来源、页面访问排行 |
| 用户指标 | 新增用户、活跃用户、留存、用户分层 |
| 登录转化 | 登录入口点击率、登录成功率、登录失败原因、注册转化率 |
| 行为漏斗 | 访问 → 点击 → 登录 → 使用 → 付费/转化 |
| 子应用表现 | 子应用访问量、使用深度、错误率、转化率 |
| 广告指标 | 请求量、填充率、曝光量、点击率、eCPM、收入 |
| 质量指标 | JS 错误率、接口错误率、白屏率、加载耗时 |

#### 6.5.2 推荐数仓分层

| 层级 | 说明 |
| --- | --- |
| ODS | 原始事件数据，尽量保留全量字段 |
| DWD | 清洗后的明细事件数据 |
| DWS | 按用户、会话、页面、广告位聚合的主题数据 |
| ADS | 面向看板和业务分析的指标结果表 |

#### 6.5.3 用户画像方向

后续可从以下维度构建用户画像：

- 注册渠道。
- 首次访问来源。
- 最近活跃时间。
- 活跃频次。
- 使用子应用偏好。
- 功能使用深度。
- 广告互动倾向。
- 付费/转化倾向。
- 风险状态。

### 6.6 广告接入

#### 6.6.1 广告位管理

每个广告位需要配置：

| 字段 | 说明 |
| --- | --- |
| adSlotId | 广告位 ID |
| appId | 所属子应用 |
| pageName | 页面名称 |
| position | 页面位置 |
| provider | 广告供应商 |
| size | 广告尺寸 |
| enabled | 是否启用 |
| refreshInterval | 刷新间隔，可选 |
| floorPrice | 底价，可选 |
| targeting | 定向参数，可选 |

#### 6.6.2 广告展示原则

- 不影响核心业务路径。
- 不遮挡登录、支付、表单等关键操作。
- 对强登录业务页面应谨慎展示广告。
- 对未成年人、敏感内容、隐私页面需要限制广告展示。
- 广告 SDK 加载失败不能影响主流程。

#### 6.6.3 广告数据归因

需要记录：

- 广告请求。
- 广告填充。
- 广告曝光。
- 广告点击。
- 广告关闭。
- 广告收益回传。
- 广告点击后的站内转化。

---

## 7. 非功能需求

### 7.1 安全

- token 不应长期存储在不安全位置。
- 敏感操作必须后端校验权限。
- 支持 token 过期和刷新机制。
- 支持统一退出和 token 失效。
- 防止 CSRF、XSS、重放攻击。
- 登录接口需要限流和风控。
- 用户隐私字段前端脱敏展示。

### 7.2 隐私与合规

- 明确告知用户数据采集目的。
- 提供隐私政策入口。
- 广告追踪和个性化推荐应支持用户授权和关闭。
- 对手机号、邮箱、IP、设备标识等敏感信息进行最小化采集和脱敏处理。
- 数据用于 BI 前需要建立数据权限和访问审计。
- 如涉及跨境用户，需要评估 GDPR、CCPA 等合规要求。

### 7.3 性能

- 登录态初始化不能明显阻塞首屏。
- 埋点 SDK 需要异步加载、批量上报、失败重试。
- 广告 SDK 延迟加载，避免影响核心页面性能。
- 子应用加载时应避免重复请求用户信息。
- 行为上报失败不能影响用户主流程。

### 7.4 可用性

- 认证服务异常时，公开页面仍应可访问。
- 强登录页面在认证服务异常时展示友好错误页。
- 埋点服务异常时本地短暂缓存，超过限制后丢弃。
- 广告加载失败时隐藏广告容器或展示兜底内容。

### 7.5 可观测性

- 登录接口成功率、失败率、耗时监控。
- token 刷新失败率监控。
- 埋点上报成功率监控。
- 广告请求、填充、曝光、点击监控。
- 子应用 JS 错误、接口错误、白屏监控。

---

## 8. MVP 范围建议

### 8.1 P0 必须实现

1. 主应用统一登录入口。
2. 用户登录、退出、登录态恢复。
3. 子应用共享登录态。
4. 路由级强登录控制。
5. 匿名 visitorId 和 sessionId。
6. 基础行为埋点：page_view、button_click、login_success、login_fail、logout。
7. 统一事件上报接口。
8. 事件公共字段标准。
9. 用户信息接口和权限基础字段。
10. 登录完成后回跳原地址。

### 8.2 P1 建议实现

1. 操作级登录拦截。
2. 页面停留时长采集。
3. JS 错误和 API 错误采集。
4. 多标签页登录态同步。
5. 事件字典管理规范。
6. 简版 BI 看板：PV、UV、登录转化、活跃用户、子应用访问排行。
7. 广告适配层设计和一个广告位试点。
8. UTM 参数采集和渠道归因。

### 8.3 P2 后续扩展

1. 第三方登录。
2. 企业 SSO。
3. 用户分群。
4. 广告收益回传。
5. 转化归因模型。
6. A/B 测试。
7. 个性化推荐。
8. 数据权限和数据资产管理。
9. 风控和异常登录检测。

---

## 9. 页面与交互需求

### 9.1 登录页/登录弹窗

登录页和登录弹窗只存在于主应用，由主应用统一维护和展示。子应用只能通过主应用提供的登录 API 或事件触发登录。

需要包含：

- 登录方式切换。
- 手机号/邮箱输入。
- 验证码获取。
- 登录按钮。
- 用户协议和隐私政策确认。
- 错误提示。
- 返回原页面。

### 9.2 未登录拦截页

适用于强登录页面：

- 告知用户当前页面需要登录。
- 提供登录按钮。
- 登录后自动回到目标页面。
- 保留用户原始访问意图。

### 9.3 无权限页面

适用于已登录但无权限：

- 告知无访问权限。
- 提供返回首页或申请权限入口。
- 记录 `permission_denied` 事件。

### 9.4 广告位展示

- 页面广告容器应预留尺寸，减少布局抖动。
- 广告加载中、加载失败、无填充状态需有兜底。
- 广告需明确可识别为广告。

---

## 10. 数据事件规范

### 10.1 命名规范

- 事件名使用小写蛇形命名，例如 `login_success`。
- 属性名使用小驼峰命名，例如 `buttonName`。
- 页面名使用稳定业务名称，不直接依赖路由路径。
- 子应用 ID 使用稳定短横线命名，例如 `user-center`。

### 10.2 事件质量要求

- 事件必须有 owner。
- 新增事件必须登记事件字典。
- 核心事件上线前需要测试验证。
- 废弃事件需要保留兼容期。
- BI 指标依赖事件不得随意变更字段含义。

### 10.3 示例事件

```json
{
  "eventId": "evt_123456",
  "eventName": "login_success",
  "eventTime": "2026-06-21T10:00:00.000Z",
  "appId": "main-shell",
  "pageName": "login_page",
  "routePath": "/login",
  "userId": "u_10001",
  "visitorId": "v_abc123",
  "sessionId": "s_xyz789",
  "utmSource": "google",
  "utmMedium": "cpc",
  "utmCampaign": "summer_campaign",
  "properties": {
    "loginMethod": "phone_code",
    "isNewUser": false,
    "sourceAppId": "landing-app"
  }
}
```

---

## 11. 技术实现建议

### 11.1 前端包建议

建议在 `tsu-cli` 生成的 MFE 工程中抽象公共包：

| 包名 | 职责 |
| --- | --- |
| `@shared/auth-sdk` | 主应用认证封装、登录态读取、用户信息、权限、登录事件、子应用登录触发入口 |
| `@shared/tracking-sdk` | 行为埋点、事件队列、上报 |
| `@shared/ads-sdk` | 广告适配、广告位渲染、广告事件 |
| `@shared/config-sdk` | 远程配置、灰度、开关 |
| `@shared/types` | 用户、权限、事件、广告等公共类型 |
| `@shared/ui` | 登录弹窗、无权限页、错误页等公共 UI |

### 11.2 Auth SDK 能力

`Auth SDK` 分为主应用能力和子应用消费能力。真实登录、退出、token 刷新只允许在主应用执行；子应用只能读取状态、判断权限、触发主应用登录。

```ts
interface MainAppAuthSDK {
  init(): Promise<void>
  login(params?: LoginParams): Promise<LoginResult>
  logout(): Promise<void>
  refreshToken(): Promise<void>
  getUser(): User | null
  getToken(): string | null
  isAuthenticated(): boolean
  hasPermission(permission: string): boolean
  requireLogin(options?: RequireLoginOptions): Promise<User>
  onAuthChange(callback: (state: AuthState) => void): () => void
}

interface SubAppAuthBridge {
  getUser(): User | null
  isAuthenticated(): boolean
  hasPermission(permission: string): boolean
  requireLogin(options?: RequireLoginOptions): Promise<User>
  onAuthChange(callback: (state: AuthState) => void): () => void
}
```

子应用禁止直接调用登录接口，禁止直接保存或刷新 token。

### 11.3 Tracking SDK 能力

```ts
interface TrackingSDK {
  init(config: TrackingConfig): void
  identify(userId: string, traits?: Record<string, unknown>): void
  alias(visitorId: string, userId: string): void
  track(eventName: string, properties?: Record<string, unknown>): void
  page(pageName: string, properties?: Record<string, unknown>): void
  flush(): Promise<void>
}
```

### 11.4 广告 SDK 能力

```ts
interface AdsSDK {
  init(config: AdsConfig): Promise<void>
  render(slot: AdSlot): Promise<void>
  destroy(slotId: string): void
  refresh(slotId: string): Promise<void>
  on(eventName: AdsEventName, callback: AdsEventCallback): void
}
```

---

## 12. 后端与数据接口建议

### 12.1 认证接口

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/auth/login` | POST | 登录 |
| `/api/auth/logout` | POST | 退出 |
| `/api/auth/refresh` | POST | 刷新 token |
| `/api/auth/me` | GET | 获取当前用户 |
| `/api/auth/permissions` | GET | 获取权限 |

### 12.2 埋点接口

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/events/collect` | POST | 单条或批量事件上报 |
| `/api/events/schema` | GET | 获取事件字典，可选 |

### 12.3 广告接口

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/ads/config` | GET | 获取广告位配置 |
| `/api/ads/report` | POST | 广告事件回传，可选 |

---

## 13. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 登录系统与子应用耦合过深 | 后续维护困难 | 通过 Auth SDK 和标准接口解耦 |
| 埋点口径不统一 | BI 数据不可用 | 建立事件字典和上线审核流程 |
| 第三方广告影响性能 | 首屏变慢、体验下降 | 延迟加载、按需加载、广告容器隔离 |
| 隐私合规不足 | 法务和平台风险 | 隐私政策、用户授权、最小化采集 |
| token 泄露 | 账号安全风险 | 安全存储、短 token、刷新机制、后端校验 |
| 数据量增长过快 | 成本上升 | 批量上报、采样、冷热分层存储 |
| 匿名与登录归因错误 | 用户分析失真 | 明确 alias 规则，避免一对多污染 |

---

## 14. 成功指标

### 14.1 用户体验指标

- 登录成功率 ≥ 95%。
- 登录后跨子应用重复登录率接近 0。
- 强登录拦截后回跳成功率 ≥ 99%。
- 登录流程平均耗时可监控并持续优化。

### 14.2 数据质量指标

- 核心事件上报成功率 ≥ 99%。
- 核心事件字段完整率 ≥ 98%。
- visitorId 覆盖率 ≥ 99%。
- 登录用户 userId 关联准确率可审计。

### 14.3 商业化指标

- 广告曝光、点击、收入可按广告位分析。
- 广告 CTR、填充率、eCPM 可监控。
- 广告对核心业务转化的影响可评估。

### 14.4 系统质量指标

- 登录接口 P95 响应时间可控。
- 埋点上报不影响主流程。
- 广告 SDK 加载失败不影响页面可用。

---

## 15. 里程碑建议

### 阶段 1：基础登录与强登录能力

- 完成主应用登录入口。
- 完成 Auth SDK MVP。
- 完成登录、退出、登录态恢复。
- 完成路由级强登录。
- 完成用户信息和权限基础接口。

### 阶段 2：统一埋点与基础 BI

- 完成 Tracking SDK MVP。
- 完成基础事件字典。
- 完成 visitorId、sessionId。
- 完成登录前后用户关联。
- 完成基础 BI 指标看板。

### 阶段 3：广告试点与商业化数据

- 完成 Ads Adapter。
- 接入一个第三方广告平台。
- 在低风险页面试点广告位。
- 完成广告曝光、点击、收入数据采集。
- 分析广告对用户体验和转化的影响。

### 阶段 4：增长与数据深化

- 用户分群。
- 漏斗分析。
- A/B 测试。
- 转化归因。
- 个性化运营。

---

## 16. 已确认信息

| 问题 | 结论 | 说明 |
| --- | --- | --- |
| 首期目标用户 | 国内 | 登录方式与合规策略按国内场景优先设计 |
| 微前端框架 | qiankun | 主应用统一承接登录态与权限控制 |
| 现有账号体系 | 没有 | 需要从头建设统一认证与用户体系 |
| BI 选型 | 自建 BI | 从项目初期建设统一事件采集、数据入仓、指标建模和基础看板能力 |
| 广告平台 | 腾讯、百度 | 预留广告适配层与事件归因能力 |
| 高价值转化事件 | 没有 | 当前优先级更偏流量、登录与行为分析 |
| 广告个性化 | 不做 | 仅做基础曝光、点击、转化归因，不做用户画像定向广告 |

### 16.1 BI 选型建议

在当前前提下，BI 采用“自建 BI”的方式，从项目初期就建设统一事件采集、数据入仓、指标建模和基础看板能力。

选择自建 BI 的原因：

- 用户行为数据、登录数据、广告数据可以统一沉淀为自有数据资产。
- 后续可以围绕用户、子应用、广告位、渠道、会话等维度灵活建模。
- 不依赖第三方平台的数据口径、事件限制和费用策略。
- 更适合后续接入腾讯、百度广告数据并做统一归因。
- 虽然当前没有支付、订单、会员等高价值转化事件，但仍可以先建设流量、登录、行为、广告四类基础指标。

自建 BI 建议按阶段落地：

- **短期**：完成统一埋点 SDK、事件采集接口、原始事件表、基础看板。
- **中期**：建设用户、会话、页面、子应用、广告位等主题模型。
- **长期**：建设指标管理、数据权限、数据质量监控和用户分群能力。

MVP 阶段不追求完整数仓平台，优先保证核心事件可采、可查、可统计、可追溯。

---

## 17. 推荐结论

建议采用“主应用统一登录态 + Auth SDK 标准化接入 + Tracking SDK 统一埋点 + Ads Adapter 平台化广告接入”的方案。

MVP 阶段优先解决：

1. 用户在多个子应用之间只登录一次。
2. 子应用可以配置强登录页面和强登录操作。
3. 登录前后用户行为能够被连续记录和关联。
4. 核心事件能够稳定进入数据链路，为 BI 提供基础数据。
5. 广告接入不直接散落在子应用中，而是通过统一广告适配层试点。

该方案既能支持当前 MFE 项目的快速落地，也能为后续用户增长、广告收入、BI 分析和数据资产建设保留扩展空间。
